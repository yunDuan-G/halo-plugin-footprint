# 悬停卡片 - 标记点置顶与突出显示方案

## 需求分析

### 需求一：标记点置顶（含关联标记点）

**状态：✅ 已实现，无需修改**

现有 `loadParabolaAnimation` 函数（第195-201行）已经实现了此功能：

```javascript
// 遍历所有标记图片（包括关联标记点）
markerImages.forEach(markerImg => {
    const markerElement = markerImg?.closest('.amap-marker');
    if (markerElement) {
        // 将card对应的标记点显示到最上层
        markerElement.classList.add('zIndex13');
    }
});
```

`handleLeave`（第793-797行）在鼠标移出时还原：

```javascript
const amapMarker = document.querySelectorAll('.amap-marker');
amapMarker.forEach(marker => {
    marker.classList.remove('zIndex13');
});
```

无论 `enableHoverZoom` 开启与否，`loadParabolaAnimation` 最终都会被调用，因此标记点置顶逻辑在所有路径上都生效。

---

### 需求二：主标记点突出显示（不含关联标记点）

用户需要在卡片悬停时，对**卡片自身的标记点**进行额外的视觉突出，关联标记点不参与。

#### 建议方案对比

| 方案 | 视觉效果 | 实现复杂度 | 性能 | 推荐度 |
|------|---------|-----------|------|--------|
| **A. 脉冲光环** | 标记点外围扩散涟漪 | 中（CSS animation + JS class toggling） | 好 | ⭐⭐⭐ |
| **B. 缩放 + 阴影** | 放大1.3倍并加彩色光晕 | 低（纯CSS） | 最优 | ⭐⭐⭐⭐⭐ |
| **C. 弹跳动画** | 小幅度上下弹跳 | 低（CSS keyframes） | 好 | ⭐⭐⭐ |
| **D. 边框高亮环** | 主题色粗圆环包裹标记 | 低（CSS box-shadow） | 最优 | ⭐⭐⭐⭐ |

---

#### 推荐方案：B — 缩放 + 彩色光晕（推荐）

**效果描述**：卡片悬停时，对应标记点放大至 1.3 倍并加上主题 HSL 色的外发光（box-shadow glow），关联标记点不受影响。

**优点**：
- 纯 CSS 实现，无 JS 动画开销
- 与现有三种标记样式（pin/circle/card）的 hover 效果一致性强
- 视觉上自然不突兀，又能明确区分

**实现思路**：
1. 在 `loadParabolaAnimation` 中，对主标记点单独添加 CSS class（如 `marker-highlight`），关联标记点不加
2. 在 `handleLeave` 中移除 `marker-highlight`
3. 在 `footprint.css` 中为 `.marker-highlight` 定义缩放+光晕样式，使用 `hsla` CSS 变量适配主题色

**CSS 示例**：
```css
.marker-highlight .marker-image {
    transform: scale(1.3);
    box-shadow: 0 0 16px 4px hsla(var(--theme-h), var(--theme-s), var(--theme-l), 0.5);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}
```

---

#### 备选方案：D — 边框高亮环

**效果描述**：在标记点外围显示一个主题色的加粗实心圆环，类似"选中"状态。

**优点**：
- 更加醒目，适合标记点较小或地图背景复杂的场景
- 兼容所有标记样式
- 与 "置顶"（z-index）搭配形成"选中态"

---

## 最终推荐

**方案 B（缩放+光晕）**作为首选，如果不满意可替换为方案 D。

## 修改文件清单

| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `src/main/resources/static/js/footprint.js` | `loadParabolaAnimation` 中为主标记点添加 `marker-highlight` class；`handleLeave` 中移除 |
| 2 | `src/main/resources/static/css/footprint.css` | 新增 `.marker-highlight` 样式（缩放+光晕） |

---

## JS 修改详情

### loadParabolaAnimation（约第194行）

在 `markerImages.forEach(...)` 循环中添加主标记点判断：

```javascript
// 当前代码：
markerImages.forEach(markerImg => {
    const markerElement = markerImg?.closest('.amap-marker');
    if (markerElement) {
        markerElement.classList.add('zIndex13');
    }
});

// 修改后：
markerImages.forEach(markerImg => {
    const markerElement = markerImg?.closest('.amap-marker');
    if (markerElement) {
        markerElement.classList.add('zIndex13');
        // 主标记点（非关联）额外突出显示
        if (markerImg === markerImage) {
            markerElement.classList.add('marker-highlight');
        }
    }
});
```

### handleLeave（约第793行）

```javascript
// 当前代码：
amapMarker.forEach(marker => {
    marker.classList.remove('zIndex13');
});

// 修改后：
amapMarker.forEach(marker => {
    marker.classList.remove('zIndex13');
    marker.classList.remove('marker-highlight');
});
```
