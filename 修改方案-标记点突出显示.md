# 悬停卡片 - 标记点置顶与突出显示方案

## 需求一：标记点置顶（含关联标记点）

### 根因分析

经排查，此功能因 markerCache 缓存写入位置错误，**从未真正生效**。

ootprint.js 第 1565-1573 行，markerCache.set() 被错误地放在了 catch 块中：

`javascript
try {
    // ...创建标记...
    map.add(marker);
} catch (error) {
    // ⚠️ 只在这里填充缓存！成功创建的标记从未写入
    const imgElement = markerContent.querySelector('img');
    if (imgElement) {
        markerCache.set(footprint.spec.name, imgElement);
    }
    console.error('创建标记失败:', error, footprint);
}
`

**调用链分析**：

`	ext
悬停卡片 → handleEnter → loadParabolaAnimation
                               ↓
                     markerCache.get("宝泉")  →  undefined（缓存中无此条目）
                               ↓
                     markerImg?.closest('.amap-marker')  →  null（短路，未执行）
                               ↓
                     zIndex13 从未添加 →  标记点始终在底层
`

> 抛物线能正常渲染是因为终点坐标用 map.lngLatToContainer() 根据经纬度计算，不依赖 markerCache。

### 修复方案

将 markerCache.set() 移至成功路径（map.add(marker) 之后）：

`javascript
map.add(marker);
// 缓存标记点DOM引用，供抛物线动画使用
const imgElement = markerContent.querySelector('img');
if (imgElement) {
    markerCache.set(footprint.spec.name, imgElement);
}
} catch (error) {
    console.error('创建标记失败:', error, footprint);
}
`

**影响面**：仅 ootprint.js 一处，移动 5 行代码。

### 修复后效果

| | 修复前 | 修复后 |
|---|---|---|
| markerCache.get("宝泉") | undefined | <img> DOM 引用 |
| closest('.amap-marker') | 未执行 | 找到 .amap-marker |
| 主标记点 zIndex13 | ❌ | ✅ |
| 关联标记点 zIndex13 | ❌ | ✅ |
| handleLeave 还原 | 无效果 | ✅ |

---

## 需求二：主标记点突出显示（不含关联标记点）

### 推荐方案：B — 缩放 + 彩色光晕

**效果**：卡片悬停时，仅卡片自身标记点放大 1.3 倍并加上主题 HSL 色外发光，关联标记点不受影响。

**CSS 示例**：
`css
.marker-highlight .marker-image {
    transform: scale(1.3);
    box-shadow: 0 0 16px 4px hsla(var(--theme-h), var(--theme-s), var(--theme-l), 0.5);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}
`

### JS 修改

**loadParabolaAnimation（在设置 zIndex13 的循环中增加判断）：**

`javascript
markerImages.forEach(markerImg => {
    const markerElement = markerImg?.closest('.amap-marker');
    if (markerElement) {
        markerElement.classList.add('zIndex13');
        // 主标记点（非关联）额外突出显示
        if (markerImg === markerImage) {
            markerElement.classList.add('marker-highlight');
        }
    }
});
`

**handleLeave（增加移除）：**

`javascript
amapMarker.forEach(marker => {
    marker.classList.remove('zIndex13');
    marker.classList.remove('marker-highlight');
});
`


## 需求二（完整版）：主标记点突出显示 — 四种方案全部实现

> 通过 `settings.yaml` 新增 `highlightScheme` 配置项，允许用户在四种高亮方案间切换。默认值为 `glow`（方案 B）。

---

### settings.yaml 新增配置项

在 `markerStyle` 选择器之后追加：

```yaml
        - $formkit: select
          name: highlightScheme
          label: 悬停高亮方案
          value: 'glow'
          options:
            - label: 缩放光晕
              value: glow
            - label: 脉冲光环
              value: pulse
            - label: 弹跳动画
              value: bounce
            - label: 边框高亮环
              value: ring
```

---

### footprint.html 注入配置

在 `FOOTPRINT_CONFIG` 对象中（`enableHoverZoom` 之后）追加一行：

```javascript
highlightScheme: /*[[${settings.highlightScheme}]]*/ 'glow',
```

完整配置尾部变为：

```javascript
window.FOOTPRINT_CONFIG = {
    // ... 其他字段 ...
    markerStyle: /*[[${settings.markerStyle}]]*/ 'circle',
    enableHoverZoom: /*[[${settings.enableHoverZoom}]]*/ false,
    highlightScheme: /*[[${settings.highlightScheme}]]*/ 'glow'
};
```

---

### JS 修改（footprint.js）

#### 1. 页面初始化时注入 scheme class

在 `DOMContentLoaded` 中（约第 18-20 行，`markerStyle` class 注入之后）追加：

```javascript
// 设置标记点样式类
if (window.FOOTPRINT_CONFIG.markerStyle) {
    document.body.classList.add('marker-style-' + window.FOOTPRINT_CONFIG.markerStyle);
}
// 设置高亮方案类
if (window.FOOTPRINT_CONFIG.highlightScheme) {
    document.body.classList.add('highlight-scheme-' + window.FOOTPRINT_CONFIG.highlightScheme);
}
```

#### 2. loadParabolaAnimation 中添加主标记点高亮 class

在设置 `zIndex13` 的循环中增加判断（原方案位置，保持不变）：

```javascript
markerImages.forEach(markerImg => {
    const markerElement = markerImg?.closest('.amap-marker');
    if (markerElement) {
        markerElement.classList.add('zIndex13');
        // 主标记点（非关联）额外突出显示
        if (markerImg === markerImage) {
            markerElement.classList.add('marker-highlight');
        }
    }
});
```

#### 3. 地图标记 handleLeave 中移除

```javascript
amapMarker.forEach(marker => {
    marker.classList.remove('zIndex13');
    marker.classList.remove('marker-highlight');
});
```

#### 4. 时间线 cleanup 中追加移除（补充优化 P0-1）

```javascript
// 移除标记点的置顶和高亮
document.querySelectorAll('.amap-marker').forEach(marker => {
    marker.classList.remove('zIndex13');
    marker.classList.remove('marker-highlight');
});
```

#### 5. handleScroll 中追加移除（补充优化 P0-2）

```javascript
document.querySelectorAll('.amap-marker').forEach(marker => {
    marker.classList.remove('zIndex13');
    marker.classList.remove('marker-highlight');
});
```

---

### CSS 修改（footprint.css）— 四种方案完整样式

#### 基础过渡（所有方案共用，补充优化 P2-1）

```css
.marker-style-pin .marker-image,
.marker-style-circle .marker-image,
.marker-style-card .marker-image {
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}
```

---

#### 方案 B：缩放光晕（默认，glow）

```css
/* ===== 方案 B：缩放 + 彩色光晕 ===== */
.highlight-scheme-glow .amap-marker.marker-highlight .marker-image,
.highlight-scheme-glow .amap-marker.marker-highlight .custom-marker:hover .marker-image {
    transform: scale(1.3);
    box-shadow: 0 0 16px 4px hsla(var(--hsla-raw), 0.5);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}
```

效果：卡片悬停时，主标记点放大 1.3 倍并加上主题 HSL 色外发光。

---

#### 方案 A：脉冲光环（pulse）

```css
/* ===== 方案 A：脉冲光环（外围扩散涟漪） ===== */
.highlight-scheme-pulse .amap-marker.marker-highlight .marker-image {
    position: relative;
    transform: scale(1.15);
    transition: transform 0.25s ease;
}

/* 脉冲波纹 */
.highlight-scheme-pulse .amap-marker.marker-highlight .marker-image::before {
    content: '';
    position: absolute;
    inset: -8px;
    border-radius: 50%;
    border: 3px solid hsla(var(--hsla-raw), 0.6);
    animation: highlight-pulse 1.2s ease-out infinite;
    pointer-events: none;
    z-index: -1;
}

@keyframes highlight-pulse {
    0% {
        transform: scale(0.8);
        opacity: 1;
    }
    100% {
        transform: scale(1.6);
        opacity: 0;
    }
}
```

效果：标记点微微放大，外围持续扩散涟漪波纹，动画感强。

---

#### 方案 C：弹跳动画（bounce）

```css
/* ===== 方案 C：弹跳动画 ===== */
.highlight-scheme-bounce .amap-marker.marker-highlight .marker-image,
.highlight-scheme-bounce .amap-marker.marker-highlight .custom-marker:hover .marker-image {
    animation: highlight-bounce 0.6s ease-in-out infinite alternate;
    box-shadow: 0 8px 24px hsla(var(--hsla-raw), 0.4);
}

@keyframes highlight-bounce {
    0% {
        transform: translateY(0) scale(1.05);
    }
    100% {
        transform: translateY(-8px) scale(1.12);
    }
}
```

效果：标记点小幅度上下弹跳，趣味性强。

---

#### 方案 D：边框高亮环（ring）

```css
/* ===== 方案 D：边框高亮环 ===== */
.highlight-scheme-ring .amap-marker.marker-highlight .marker-image,
.highlight-scheme-ring .amap-marker.marker-highlight .custom-marker:hover .marker-image {
    transform: scale(1.15);
    box-shadow:
        0 0 0 4px hsla(var(--hsla-raw), 0.3),
        0 0 0 8px hsla(var(--hsla-raw), 0.15);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}
```

效果：标记点外围双层主题色粗圆环，醒目直接，适合标记点较小的场景。

---

### 方案对比

| 方案 | value | 视觉效果 | 适用场景 |
|------|-------|---------|---------|
| B 缩放光晕 | `glow` | 放大 + 主题色外发光 | **默认推荐**，通用性好 |
| A 脉冲光环 | `pulse` | 微微放大 + 外围扩散涟漪 | 需要强调动画感的页面 |
| C 弹跳动画 | `bounce` | 上下弹跳 + 阴影 | 活泼风格，可能喧宾夺主 |
| D 边框高亮环 | `ring` | 双层主题色圆环 | 标记点较小、需要醒目提示 |

---

## 修改文件清单（最终版）

| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `src/main/resources/extensions/settings.yaml` | ① 在 `markerStyle` 之后新增 `highlightScheme` select 配置项 |
| 2 | `src/main/resources/templates/footprint.html` | ① `FOOTPRINT_CONFIG` 中注入 `highlightScheme` 字段 |
| 3 | `src/main/resources/static/js/footprint.js` | ① 将 `markerCache.set()` 从 catch 移至成功路径（修复 Bug） |
| | | ② 初始化时注入 `highlight-scheme-*` body class |
| | | ③ `loadParabolaAnimation` 中为主标记点添加 `marker-highlight` class |
| | | ④ 地图标记 `handleLeave` 中移除 `marker-highlight` |
| | | ⑤ 时间线 `cleanup` 中追加 `zIndex13`/`marker-highlight` 移除 |
| | | ⑥ `handleScroll` 中追加 `zIndex13`/`marker-highlight` 移除 |
| 4 | `src/main/resources/static/css/footprint.css` | ① 三种标记风格基础 `.marker-image` 添加 `transition` |
| | | ② 方案 B（glow）样式 |
| | | ③ 方案 A（pulse）样式 + `@keyframes highlight-pulse` |
| | | ④ 方案 C（bounce）样式 + `@keyframes highlight-bounce` |
| | | ⑤ 方案 D（ring）样式 |

> **修改量**：settings.yaml 1 处（约 10 行），footprint.html 1 处（1 行），JS 6 处（共约 16 行），CSS 约 55 行。

---

## 补充优化建议（第二轮审核）

> 以下为四方案全部实现后的进一步审查发现。

### P1：Pulse 方案在 `overflow: hidden` 下失效

**根因**：三种标记风格的 `.marker-image` 均有 `overflow: hidden`（第 681/729/794 行），而 Pulse 方案的 `::before` 伪元素使用 `inset: -8px` 向外扩展，超出部分会被裁剪，脉冲波纹不可见。

`css
/* pin 风格（第 681 行）：overflow: hidden */
.marker-style-pin .marker-image {
    overflow: hidden;  /* ⚠️ 会裁剪 ::before */
    ...
}
/* circle 风格（第 729 行）：同样有 overflow: hidden */
/* card 风格（第 794 行）：同样有 overflow: hidden */
`

**修复**：在 Pulse 方案 CSS 中覆盖 `overflow`：

`css
.highlight-scheme-pulse .amap-marker.marker-highlight .marker-image {
    overflow: visible;  /* 允许 ::before 波纹溢出 */
    transform: scale(1.15);
    transition: transform 0.25s ease;
}
`

---

### P2-1：Card 风格下 Pulse/Ring 方案形状不匹配

**问题**：Card 标记的 `.marker-image` 为 50×36 矩形（`border-radius: 4px`），而非正方形。

- **Pulse 方案**：`border-radius: 50%` 在矩形上产生椭圆形波纹，与圆形设计意图不符
- **Ring 方案**：`box-shadow` 跟随矩形轮廓，产生圆角矩形高亮环，非圆形光环

**影响**：视觉效果在 Card 风格下与预期有差距，但不影响功能。建议在文档中注明此限制，或在 Card 风格下对 Pulse/Ring 做针对性调整：

`css
/* Card 风格 Pulse 适配 */
.highlight-scheme-pulse .marker-style-card .amap-marker.marker-highlight .marker-image::before {
    border-radius: 8px;  /* 匹配 card 的 4px border-radius + 扩展 */
}
/* Card 风格 Ring 适配 */
.highlight-scheme-ring .marker-style-card .amap-marker.marker-highlight .marker-image {
    border-radius: 8px;
}
`

---

### P2-2：Bounce 方案移除时无退出过渡

**问题**：`animation` 属性不支持 CSS transition。当 `marker-highlight` class 移除时，`animation` 直接被取消，标记点瞬间跳回原位，缺乏平滑退出。

**修复思路**：使用两层 class 控制——`marker-highlight` 触发动画，额外用一个 wrapper 控制淡出：

`css
/* 动画播放中 */
.highlight-scheme-bounce .amap-marker.marker-highlight .marker-image {
    animation: highlight-bounce 0.6s ease-in-out infinite alternate;
}
/* JS 移除前先切到此状态，transition 接管退出 */
.highlight-scheme-bounce .amap-marker.marker-highlight-exiting .marker-image {
    animation: none;
    transform: translateY(0) scale(1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0 2px 8px var(--shadow-color);
}
`

> JS 侧配合：移除前先替换为 `marker-highlight-exiting`，`transitionend` 后再移除 class。实现复杂度较高，可按需采纳。

---

### P3-1：基础 `transition` 规则改变 Circle/Card 原有行为

**问题**：方案中在所有 `.marker-image` 上统一添加 `transition`：

- **Pin**：已有 `transition: var(--transition)`，无影响
- **Circle**：仅有 `transition: box-shadow 0.2s ease`，新增 `transform` transition 会改变 hover 行为（原本 hover 在 `.custom-marker` 上做 scale，`.marker-image` 不参与；新增后 `.marker-image` 的 transform 也会有 0.25s 过渡）
- **Card**：原本无 transition，新增后所有 transform/box-shadow 变化均有过渡

**建议**：这是合理增强，但应在方案中明确记录这一行为变更，避免被误认为 bug。

---

### P3-2：建议添加 `prefers-reduced-motion` 无障碍支持

**建议**：对 Pulse 和 Bounce 等动画方案，当用户系统开启"减少动效"时，应降级为静态高亮（同 Glow 效果）：

```css
@media (prefers-reduced-motion: reduce) {
    .highlight-scheme-pulse .amap-marker.marker-highlight .marker-image::before {
        animation: none;
        display: none;
    }
    .highlight-scheme-bounce .amap-marker.marker-highlight .marker-image {
        animation: none;
        transform: scale(1.15);
        box-shadow: 0 0 16px 4px hsla(var(--hsla-raw), 0.5);
    }
}
```

---

### P3-3：Pulse 方案与 Pin 风格 `::after` 共存确认

Pin 风格的 `.marker-image::after` 已用于扩展点击热区（`inset: -8px; background: transparent; z-index: -1`）。Pulse 方案使用 `::before`，两者共存：
- 层叠顺序：`::before` → `::after`（均为 `z-index: -1`）
- `::after` 为透明背景，不会遮挡 `::before` 的波纹
- ✅ 无冲突，但需确认叠加后视觉效果

---

## 修改文件清单（最终版 v2）

| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `src/main/resources/extensions/settings.yaml` | ① 在 `markerStyle` 之后新增 `highlightScheme` select 配置项 |
| 2 | `src/main/resources/templates/footprint.html` | ① `FOOTPRINT_CONFIG` 中注入 `highlightScheme` 字段 |
| 3 | `src/main/resources/static/js/footprint.js` | ① 将 `markerCache.set()` 从 catch 移至成功路径（修复 Bug） |
| | | ② 初始化时注入 `highlight-scheme-*` body class |
| | | ③ `loadParabolaAnimation` 中为主标记点添加 `marker-highlight` class |
| | | ④ 地图标记 `handleLeave` 中移除 `marker-highlight` |
| | | ⑤ 时间线 `cleanup` 中追加 `zIndex13`/`marker-highlight` 移除 |
| | | ⑥ `handleScroll` 中追加 `zIndex13`/`marker-highlight` 移除 |
| 4 | `src/main/resources/static/css/footprint.css` | ① 三种标记风格基础 `.marker-image` 添加 `transition` |
| | | ② 方案 B（glow）样式 |
| | | ③ 方案 A（pulse）样式 + `overflow: visible` + `@keyframes` |
| | | ④ 方案 C（bounce）样式 + `@keyframes` |
| | | ⑤ 方案 D（ring）样式 |
| | | ⑥ `@media (prefers-reduced-motion)` 降级规则 |

> **修改量**：settings.yaml 1 处（约 10 行），footprint.html 1 处（1 行），JS 6 处（共约 16 行），CSS 约 70 行。
