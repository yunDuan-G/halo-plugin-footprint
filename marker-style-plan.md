# 地图标记点样式修改方案

## 当前样式

```
     ┌────┐
     │ 图 │  ← 42px 圆形图片，白边框 + 阴影
     │ 片 │
     └──┬─┘
        ▼      ← 水滴形尾巴（border-radius 不对称 + rotate(-45deg)）
```

CSS：
- 水滴形（teardrop/pin）轮廓：`border-radius: 50% 50% 50% 0.5em; transform: rotate(-45deg)`
- 内部圆形图片：42×42px，2px 白边框，阴影
- 整体白色底，4px 白边框

---

## 方案

### 方案 A：简约圆形标记（推荐 🔥）

去掉水滴形尾巴，改为纯圆形+细边框+轻阴影，现代极简。

```
     ┌────┐
     │ 图 │  ← 纯圆形，无尾巴
     │ 片 │     细主色调边框
     └────┘     轻阴影
```

| 属性 | 当前 | 改为 |
|------|------|------|
| 形状 | 水滴形 | 正圆形 |
| 尺寸 | 42px | 40px |
| 边框 | 4px 白色 | 3px `hsl(var(--hsla-raw))` |
| 阴影 | `0 2px 8px` | `0 2px 10px rgba(0,0,0,0.18)` |
| hover | 无 | 放大 1.15× + 阴影加深 |

### 方案 B：定位点风格

仿地图 App 的定位标记，圆形+外圈脉冲环。

```
    ╭──────╮
    │ ╭──╮ │  ← 外圈：半透明主色调，脉冲动画
    │ │图│ │  ← 内圈：图片
    │ ╰──╯ │
    ╰──────╯
```

- 40px 图片 + 4px 间距 + 外圈环
- 外圈使用主色调 30% 透明度
- 可选呼吸动画（pulse）
- hover 时外圈变实心

### 方案 C：卡片式标记

图片上方叠加名称标签，更像地点卡片。

```
    ┌──────────┐
    │  西湖    │  ← 名称标签
    ├──────────┤
    │   图片   │  ← 圆角矩形图片
    └────┬─────┘
         ▼         ← 小三角箭头
```

- 顶部标签栏（半透明黑底白字）
- 下方圆角矩形图片（50×36px）
- 底部小三角指向坐标点
- 适合名称重要的场景

### 方案 D：徽章/勋章风格

圆形图片 + 底部类型图标，更像收集勋章。

```
      ┌────┐
      │ 图 │  ← 圆形图片，金色边框
      │ 片 │
      └──┬─┘
       ┌┴┐     ← 小徽章：足迹类型图标
       └─┘
```

- 图片 38px，2px 金色/主色调边框
- 底部叠加小圆形徽章（足迹类型首字或图标）
- "已收集"的满足感

### 方案 E：光晕标记

圆形图片 + 底部发光投影，夜间/暗色地图上显眼。

```
      ░░░░░░░
     ░░┌──┐░░
     ░░│图│░░  ← 外发光
     ░░│片│░░
     ░░└──┘░░
      ░░░░░░░
```

- 纯圆形 40px
- `box-shadow` 多层叠加：外发光 + 投影
- 主色调光晕
- 深色地图上效果最佳

---

## 推荐组合

**方案 A（极简圆形）+ 主色调边框** — 视觉干净、现代感强、与白色统计面板协调。

如需更多个性化，可在方案 A 基础上叠加方案 D 的徽章元素。

---

## 涉及文件

| 文件 | 改动 |
|------|------|
| `footprint.css` | `.custom-marker` / `.marker-image` 样式 |
| `footprint.js` | 无需改动（除非方案 C 需要改 HTML 结构） |

---

请确认方案后开始修改。