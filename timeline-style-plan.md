# 抽屉时间线样式修改方案

## 当前状态

```
┌──────────────────────────────────┐
│  旅行时间线                    X │  ← drawer-header
├──────────────────────────────────┤
│  ┌──────┐                       │
│  │ 日期 │  ┌─────────────────┐  │
│  └──────┘  │                 │  │
│     │      │   图片 220px    │  │
│     │      │   位置 + 描述   │  │
│     │      └─────────────────┘  │
│  ┌──────┐                       │
│  │ 日期 │  ┌─────────────────┐  │
│  └──────┘  │   图片 220px    │  │
│            └─────────────────┘  │
└──────────────────────────────────┘
```

- 抽屉右侧滑入，500px 宽，毛玻璃背景
- 左侧日期胶囊 + 右侧内容卡片竖排
- 卡片内图片在上，位置/描述在下
- 有一条竖线连接各卡片

---

## 改进方案

### 方案 A：卡片间距与阴影优化

- 卡片间距从 26px → 32px，呼吸感更强
- 阴影加深，hover 时上浮 4px + 阴影扩散
- 日期胶囊改为实心主题色背景，更醒目

### 方案 B：连接线美化

- 竖线改为渐变色（主题色 → 透明），粗细 2px → 3px
- 每个卡片左侧加圆点节点（主题色实心圆）
- 日期胶囊与节点对齐

### 方案 C：图片比例统一 + 圆角

- 图片从 `height: 220px` 改为 `aspect-ratio: 16/10`
- 卡片圆角从 8px → 12px
- 图片顶部的媒体覆盖层圆角跟随卡片

### 方案 D：描述文字优化

- 描述从固定高度 `height: 20px` 改为 `max-height` + 自动截断
- 行高从 1.5 → 1.6，阅读更舒适
- 颜色对比度增强

### 方案 E：移动端全面优化

- 卡片宽度从 `65vw` → `80vw`，充分利用屏幕
- 日期胶囊左内边距缩小
- 抽屉头部高度压缩
- 图片高度从 160px → 140px

---

## 推荐组合

**方案 B（连接线美化）+ 方案 D（描述优化）+ 部分 A**：视觉上最明显的提升，连接线+节点让时间线更有"时间轴"的感觉，描述文字更易读。

---

## 涉及文件

| 文件 | 改动 |
|------|------|
| `footprint.css` | 时间线相关 CSS 样式修改 |

JS 无需改动，纯 CSS 优化。

---

请确认方案后开始修改。