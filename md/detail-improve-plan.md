# 省份详情 & 城市详情展示优化建议

## 当前状态

- 两个卡片并排展示，各 `max-h-64` 滚动列表
- 省份详情：名称 + 部分城市名 + 足迹数
- 城市详情：名称 + 所属省份 + 足迹数
- 按足迹数量降序排列

---

## 优化方案（按优先级排列）

### 方案 A：省份分类分组 + 进度条（推荐）

**改动量：小 | 视觉效果提升：大**

**省份详情**：
- 按类别分组展示：`直辖市` → `自治区` → `特别行政区` → `省份`，每组有标题分隔
- 每行增加进度条，显示该省份足迹数占总足迹数的比例
- 城市列表显示全部城市名（去掉截断，或改为"X 个城市"）

**城市详情**：
- 每行增加进度条
- 显示该城市所属省份

```
┌─ 省份详情 ─────────────────────────┐
│ 直辖市                               │
│ 北京市  ████████░░░░  3 条 (15%)    │
│ 上海市  ████░░░░░░░░  2 条 (10%)    │
│ 自治区                               │
│ 西藏    ████████████  4 条 (20%)    │
│ ...                                  │
└──────────────────────────────────────┘
```

### 方案 B：点击省份/城市筛选下方表格

**改动量：小 | 交互提升：大**

- 点击省份详情中的某省 → 下方表格自动筛选该省的足迹
- 点击城市详情中的某市 → 下方表格自动筛选该市的足迹
- 选中的省份/城市高亮显示，再次点击取消筛选
- 在搜索栏中显示当前筛选条件，可一键清除

### 方案 C：详情卡片内搜索过滤

**改动量：小**

- 省份/城市详情卡片顶部增加搜索输入框
- 输入文字即时过滤列表
- 适用于省份/城市较多时快速定位

### 方案 D：Tab 切换替代并排展示

**改动量：中**

- 省份和城市详情改为 Tab 切换（节省空间）
- 详情卡片可以更宽，展示更丰富的信息
- 每个 Tab 显示对应的统计摘要

### 方案 E：可视化排名（条形图/排行榜样式）

**改动量：中**

- 采用排行榜样式（如 Top 10 徽章）
- 前 3 名用金/银/铜标识
- 足迹数量用横向条形图展示（而非数字）
- 视觉冲击力更强

### 方案 F：省份详情中展示城市覆盖率

**改动量：中 | 需要后端支持**

- 每个省份行显示"去过城市数 / 该省总城市数"
- 如"浙江省 5/11 个城市"
- 需要后端提供各省城市总数，或前端硬编码

---

## 个人推荐

**首推：A + B 组合**，改动量小但效果显著：

| 改动点 | 说明 |
|--------|------|
| 省份按类别分组 | 与统计卡片呼应，一眼看出去过哪些直辖市/自治区 |
| 进度条 | 直观展示各省级单位的足迹占比 |
| 点击筛选表格 | 打通详情和表格，交互闭环 |
| 城市全名展示 | 去掉"等"截断或改为"共 X 个城市" |

---

请选择要实施的方案（可多选），确认后开始修改。