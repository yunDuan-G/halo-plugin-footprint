# 更新日志

## 2.9.6（2026-09-07）

### 城市卡片墙（新增）

- 顶部导航新增“城市”入口，打开全部已到访城市的卡片墙总览；
- 每张城市卡片展示封面（优先取最近去过且有图片的足迹）、省份、足迹/照片数量、票根数量与最近去过时间；
- 城市按最近去过时间倒序排列；点击卡片直接进入该城市的图片墙并自动选中；
- 支持“返回地球”/Esc 关闭，窄屏自动切换为单列卡片，无数据时显示空状态。
- 滚动条改为与城市图片墙一致的细长半透明风格，右侧不再出现浏览器默认粗滚动条。
- 修复从城市卡片墙进入城市图片墙后顶部导航残留的问题：打开/关闭时直接控制导航显隐，不再依赖相机事件。
- 城市卡片墙与城市图片墙改为两级浏览：点卡片后图片墙从卡片墙上平滑展开，返回时回到卡片墙并保留滚动位置，不再整页硬切。
- 后台「3D 地球」设置新增「移动端城市卡片墙每排城市数」（1-3，默认 2），手机端城市卡片墙按配置分列展示。
- 移动端城市卡片墙每排超过 1 个时切换为紧凑照片卡：图片改为 4:3 放大展示，只保留城市名与照片数量，收起省份与最近日期等次要文字，避免图文比例失衡。
- PC 端城市卡片墙与图片墙一致：顶栏与卡片区限制为居中 1440px 宽度，大屏下左右留白，不再横跨整个屏幕。
- 图片灯箱改为与城市图片墙/卡片墙一致的米白浅色风格（背景、按钮、底部信息条同步换肤）；
- 移除灯箱底部的“在地图上查看”按钮及相关跳转逻辑。

### 创意功能（3 / 4 / 5 / 6）

- 后台「3D 地球」新增 4 个开关：去年今天、足迹洞察、PC 城市卡片悬停轮播、旅行明信片（默认全部开启）；
- 「去年今天」：城市卡片墙顶栏在历史同月同日有足迹时显示入口，可回看照片、票根或进入图片墙；
- 「足迹洞察」：根据足迹自动生成总结文案与里程/季节/类型等指标，支持“换一句”；
- PC 城市卡片悬停轮播：鼠标悬停或键盘聚焦时最多 3 张封面淡切，滚动期间自动暂停；
- 「旅行明信片」：每张城市卡生成竖版明信片，图床不支持 CORS 时自动降级为文字排版版，支持下载与系统分享。
- 修复 PC 悬停轮播对多数城市不生效的问题：预览集改为从该城市足迹相册中取最多 3 张不重复照片，单足迹城市也可轮播。
- 优化 PC 卡片墙滚动：滚动期间不启动轮播、不预载后续图，滚动停止后悬停的卡片才加载并淡切，避免连续滚动触发图片批量解码。
- 城市卡片墙桌面端恢复显示“N 个足迹 · M 张照片”；手机端按需求只显示照片数量，不再显示足迹数量。
- 城市卡片墙打开后新增后台空闲预热：以每批 2 张的速度预解码封面与轮播图（上限 60 张），刷新后滚动不再等待图片临时解码。
- 城市卡片封面/轮播图复用后台图片处理 from→to 规则生成缩略 URL（如 `!w100`→`!A100`），避免滚动时解码/栅格 3000px 级原图。

### 城市图片墙移动端布局

- 移动端顶部压缩为「返回 / 城市名 / 切换箭头」一行，移除会挤压错位的眉题与统计文字；
- 移除顶部栏与图片墙之间约 130px 的空档，足迹目录紧跟顶部、图片墙紧随其后；
- 手账图片墙移动端改为每排 2 张的等宽两列布局，不再显示挤压的三列小图。
- 移动端隐藏图片墙顶部右侧的上一城市/下一城市切换按钮，顶部只保留返回与城市名（点击城市名仍可下拉切换城市）。
- 修复移动端足迹目录无法横向拖动的问题：目录条改为撑满整行并支持触屏横向滚动；
- 足迹目录移动端改为浅色卡片文字配色，修复白字残留在米色背景上看不清的问题。
- 移动端足迹目录不再显示照片数量，日期与数量不再粘连；
- 移动端手账标题区重排：足迹名称与时间改为上下结构，时间不再孤立地悬在名称右侧，并隐藏重复的张数行。

### 城市卡片墙滚动性能优化

- 城市卡片封面改为浏览器原生懒加载并按需异步解码，只有进入视口的图片才会请求；
- 卡片加入 `content-visibility` 跳过屏幕外卡片的布局与绘制，滚动只处理可见区域；
- 图片加载中先显示渐变底色与城市首字，就绪后淡入，避免封面突然跳出的视觉抖动。
- 打开城市卡片墙/城市图片墙等不透明页面时暂停 3D 地球渲染循环，关闭后自动恢复，避免后台满帧率渲染抢占滚动资源；
- 封面图改为进入视口后按每帧限量进入解码队列（避免滚动瞬间大批图片同时解码/栅格化），并在关闭页面时清理队列。
- 移除城市卡片的 `content-visibility` 强制跳过机制：Trace 显示它会反复丢弃/重建卡片渲染并触发同一批封面重复解码，对十余张卡片的墙属于负优化；
- 票根角标去掉 `backdrop-filter` 模糊，改用高不透明度底色，避免滚动时每个角标持续产生模糊图层开销。

## 2.9.4（2026-09-06）

> 自 v2.9.1 发布以来积累的改动统一归档到本次版本，涵盖 3D 地球、票根、图片墙与图片处理配置。

### 图片处理配置（本次新增）

- 后台新增“图片处理配置”分组，可分别配置**地图标记点**与**时间线/详情**两类图片的 URL 规则；
- 每类支持「要替换的片段 / 替换为 / 追加后缀」三段式配置，默认全部留空，直接使用足迹中保存的原始图片 URL，兼容任意图床；
- 移除代码中写死的阿里云 `!A100`、`/fw/200`、`/fw/500` 以及 `lik.cc` loading 占位图，统一改为配置驱动并使用本地 SVG 占位图；
- 同时清理足迹数据加载失败时回退到内置样例的逻辑：没有足迹数据时不再加载/构建任何标记。

### 3D 地球与票根页

- 票根标题右侧支持城市/省份拼音（按 adcode 映射），可选用 AlexBrush 手写字体；
- 票根页顶部去除重复的票根数量与关闭按钮，只保留统计汇总和“返回地球”/Esc 关闭；
- 横向长串票根优化拖动、滚动吸附与移动端提示，城市/时间信息不再错位；
- 修复刷新直达票根页时“返回地球”出现焦点边框的问题；
- 修复手机端“工具”折叠菜单点击无响应的问题。

### 足迹卡片与图片墙

- 足迹卡片与城市卡片操作改为简约文字式按钮，可配置四宫格图标；
- 足迹详情卡新增“打开图片墙”，直接进入城市卡同款图片墙并自动定位到当前足迹；
- 城市图片墙顶部新增城市下拉列表（杂志目录风格），支持快速切换城市；
- 图片墙手账照片改为滚动到底自动加载，移除手动“加载更多”按钮；
- 足迹卡主图增加“共 N 张”角标，作为图片墙快捷入口；
- 标记悬停样式区分：城市标记保留引导线，足迹标记改为贴身小气泡；
- 票根/足迹详情等页面顶部与按钮视觉统一为透明渐变与轻量描边风格。

## 2.9.1（2026-09-03）

### 顶部导航优化

- 顶部导航栏改为透明渐变、贴合 3D 地球主视觉的风格，工具栏按钮以细线分隔分组；
- 统一调整按钮间距、品牌区与底图菜单面板的视觉样式，窄屏下支持自动换行。

### 票根页移动端优化

- 票根页移动端改为整屏不滚动布局：票根固定 16:9 展示区置顶，信息区在下方完整展示；
- 票夹两侧预留扇形展开空间，切到第 2 张后，第 3 张及以后的票根不再被屏幕左右裁切遮挡；
- 点击票根切换时不再出现移动端默认的蓝色高亮框；
- 移动端票根下方新增“左右滑动切换票根”小字提示。

## 2.9.0（2026-09-02）

### 票根页全新“票夹”交互

- 移除上一张/下一张切换按钮，改为票夹式叠放：当前票根完整展示，其余票根叠在后方，底部露出边缘。
- 支持多种切换方式：
  - 点击后方露出的票根边缘，翻到前面；
  - 点击当前票根，轮换到下一张；
  - 鼠标滚轮切换；
  - 移动端手指在票根区域左右滑动切换（信息区滑动不触发）；
  - 键盘方向键切换。
- 票根叠放按底部对齐，露出高度自适应，多张票根呈扇形层次。

### 票根图片显示修复

- 修复单张票根时图片被压缩为 0 宽、加载成功却不显示的问题（网格自动布局跳过隐藏按钮导致内容列塌陷）。
- 去除票根图片的阴影、占位空盒、元素背景层：透明票根直接透出页面背景，不再出现任何“盒子”效果。
- 加载中/加载失败提示改为紧凑文字，不留空占位区域。

### 票根页背景与地球联动

- 票根页背景改为透明，直接透出 3D 地球与星空。
- 打开票根页时自动隐藏地球左侧标题卡，关闭后自动恢复。
- 打开票根页时若自动旋转未开启则自动开启地球自转。
- 打开票根页时隐藏左下角“地图数据 © 高德 / 天地图”版权信息。
- 顶部栏改为与主页一致的轻渐变透明风格，不再使用深色毛玻璃面板。

### 直达票根页

- 支持通过 `/footprints?view=tickets` 直接进入票根页；
- 打开/关闭票根页时同步地址，浏览器前进/后退可在票根页与地球页之间切换。

### 移动端优化

- 3D 地球左侧标题卡在移动端收拢到左上角并缩小字号，描述限两行，避免遮挡地球主体。
- 票根页移动端票根置顶显示、限制高度，信息在下方完整展示，尽量一屏放下。
- 移动端提示文案适配触摸设备。

### 其他修复

- 补充 Cesium 1.120 缺失的 `IAU2006_XYS` 地球定向数据文件，修复控制台 404 报错。
